1.Router => Giúp quản lý và chia các router theo từng module

2.Middleware => Xử lý dữ liệu đầu vào từ req(params, body) => Validate đầu vào

3.Controller => Thực hiện logic thao tác với database (thêm , sửa, xóa, xem)

4.Model => Các scheme database (cấu trúc của database)